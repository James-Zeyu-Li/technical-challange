# Reliable Data Pipeline — Take-Home Submission

The solution lives in [`aggregator/`](aggregator/), including source code, tests, `SPEC.md`, `PLAN.md`, `AI_USAGE.md`, and `README.md` (setup/run/test instructions).

The provided mock API service is in [`mock-api-service/`](mock-api-service/) (unmodified).
